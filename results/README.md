# Results

Approved experiment runs are written to timestamped subdirectories here. Each
run preserves its manifest, one JSONL record per assignment, deterministic
summary metrics, and every raw model response.

The pilot gate does not create an experiment run or make network calls.
